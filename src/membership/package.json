{
  "name": "membership",
  "version": "0.0.1",
  "description": "Membership module for handling user authentication and onboarding.",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel lint:watch test:watch",
    "test": "mocha --reporter spec --exit test/ client/test admin/test merchant/test",
    "test:watch": "npm run test -- --watch",
    "security-check": "nsp check",
    "lint": "esw app.js --color",
    "lint:watch": "npm run lint -- --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {}
}
